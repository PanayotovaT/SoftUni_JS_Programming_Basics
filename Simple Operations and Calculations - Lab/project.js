function project(name,numberOfProjects){
 let timeNeeded = numberOfProjects * 3;
 console.log(`The architect ${name} will need ${timeNeeded} hours to complete ${numberOfProjects} project/s.`);

}

project("Sanya", "9")